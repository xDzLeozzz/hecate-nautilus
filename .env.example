# =============================================================
# === ARQUIVO DE AMBIENTE E RECURSOS - PROJETO HÉCATE-NAUTILUS ===
# =============================================================
#
# Copie este arquivo para .env (cp .env.example .env) e ajuste os
# valores abaixo para corresponder ao seu hardware.
#
# -------------------------------------------------------------
# Versão do Unbound a ser compilada
UNBOUND_VERSION=1.19.3

# -------------------------------------------------------------
# Alocação de CPU (cpuset)
# Define quais núcleos da CPU cada serviço pode usar.
# Exemplo para um CPU de 4 núcleos (0-3): UNBOUND_CPUSET="1-3", ADGUARD_CPUSET="0"
#
# Otimizado para Xeon E5-2680v4 (14 Cores / 28 Threads)
UNBOUND_CPUSET="2-12"
ADGUARD_CPUSET="0-1"
REDIS_CPUSET="13"

# -------------------------------------------------------------
# Limites de Memória (mem_limit)
# Define o máximo de RAM que cada serviço pode consumir.
#
# Otimizado para 16GB RAM
UNBOUND_MEM_LIMIT="10g"
ADGUARD_MEM_LIMIT="2g"
REDIS_MEM_LIMIT="2g"

# -------------------------------------------------------------
# (Opcional) ID de Usuário e Grupo para execução não-root
# Garante que os arquivos criados tenham a permissão correta.
# Para descobrir seu UID/GID no Linux, use o comando 'id -u' e 'id -g'.
# No Docker Desktop para Windows, o padrão 1000:1000 é geralmente seguro.
# HECATE_UID=1000
# HECATE_GID=1000